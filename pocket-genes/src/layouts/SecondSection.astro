<section id="screen2" class="interactive">
  <div class="text">
    <h1 class="glow-text">Mobile & Interactive Genomic Panels</h1>
    <p class="glow-text">
      Clear reports on health, nutrition, ancestry, and more—making genomics
      easy to explore and understand for your users.
    </p>
  </div>
  <div class="image-wrapper">
    <div class="image-card">
      <div class="lightning"></div>
      <img src="mobile3.png" alt="Pocket Genes App" />
    </div>
  </div>
  <div class="wave"></div>
  <!-- Wave effect for smooth transition -->
</section>

<style>
  .interactive {
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0vh 10vw;
    position: relative;
    background-color: #1a36b4;
    overflow: hidden;
  }

  /* TEXT STYLES */
  .interactive .text {
    width: 65%;
    color: #e0e5ff;
    position: relative;
  }

  .interactive h1 {
    font-size: 4.5rem;
    font-weight: bold;
    color: #ffffff;
    margin-block-start: 0;
    margin-block-end: 8rem;
    transition:
      text-shadow 0.4s ease-in-out,
      transform 0.4s ease-in-out; /* Smooth transition back */
  }

  .interactive p {
    font-size: 2.5rem;
    font-weight: 700;
    padding-left: 2rem;
    line-height: 1.4;
    color: #afc3ff;
    max-width: 70%;
    transition:
      text-shadow 0.4s ease-in-out,
      transform 0.4s ease-in-out; /* Smooth transition back */
  }

  /* TEXT GLOW EFFECT & MOVEMENT */
  .glow-text:hover {
    text-shadow: 0px 0px 8px rgba(255, 255, 255, 0.5);
    transform: scale(1.02) translateY(-3px);
  }

  /* IMAGE WRAPPER */
  .image-wrapper {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  /* IMAGE CARD CONTAINER */
  .image-card {
    width: 85%;
    max-width: 450px;
    position: relative;
    border-radius: 15px;
    transition:
      transform 0.4s ease-in-out,
      box-shadow 0.4s ease-in-out;
    overflow: hidden;
    position: absolute;
    bottom: 170px;
    right: 150px; /* Moved left by 100px */
    transform: rotate(10deg);
  }

  /* IMAGE STYLES */
  .image-card img {
    width: 100%;
    border-radius: 10px;
  }

  /* HOVER EFFECT: CARD MOVES UP */
  .image-card:hover {
    transform: rotate(10deg) translateY(-12px) scale(1.05);
    box-shadow: 0px 15px 40px rgba(0, 0, 0, 0.3);
  }

  /* LIGHTNING SHIMMER EFFECT */
  .lightning {
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      to right,
      rgba(255, 255, 255, 0) 0%,
      rgba(255, 255, 255, 0.8) 50%,
      rgba(255, 255, 255, 0) 100%
    );
    transform: skewX(-20deg);
    transition: left 0.3s ease-out;
  }

  /* LIGHTNING MOVES FAST ON HOVER */
  .image-card:hover .lightning {
    left: 120%;
  }

  /* WAVE EFFECT */
  .wave {
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 100%;
    height: 75px;
    background: url("wave.svg");
    background-repeat: no-repeat;
    background-size: cover;
    animation: wave-animation 4s infinite linear;
  }

  /* WAVE ANIMATION */
  @keyframes wave-animation {
    from {
      background-position-x: 0px;
    }
    to {
      background-position-x: 100px;
    }
  }

  /* END TO */
  /* ANIMATIONS */
  /* ANIMATIONS */
  /* ANIMATIONS */
  .fade-in {
    opacity: 0;
    transform: translateY(50px);
  }

  .fade-in.show {
    opacity: 1;
    transform: translateY(0);
  }

  @media screen and (max-width: 580px) {
    .interactive {
      flex-direction: column;
      width: auto;
      padding: 0vh 5vw;
    }

    .interactive .text {
      width: 90%;
    }

    .interactive h1 {
      padding-top: 2rem;
      margin-block-end: 4rem;
      font-size: 2.5rem;
    }

    .interactive p {
      font-size: 1.75rem;
    }

    /* IMAGE CARD CONTAINER */
    .image-card {
      bottom: 50px;
      left: 100px;
      width: 45%;
      rotate: 0deg;
    }

    /* IMAGE STYLES */
    .image-card img {
      width: 85%;
      border-radius: 10px;
    }
  }

  @media (min-width: 581px) and (max-width: 800px) {
    .interactive {
      flex-direction: column;
      width: auto;
      padding: 0vh 5vw;
    }

    .interactive .text {
      width: 100%;
    }

    .interactive h1 {
      padding-top: 2rem;
      margin-block-end: 4rem;
      font-size: 2.5rem;
    }

    .interactive p {
      font-size: 1.75rem;
    }

    /* IMAGE CARD CONTAINER */
    .image-card {
      bottom: 100px;
      left: 100px;
      width: 55%;
      rotate: 0deg;
    }

    /* IMAGE STYLES */
    .image-card img {
      width: 80%;
      border-radius: 10px;
    }
  }
  @media (min-width: 801px) and (max-width: 1050px) {
    .interactive {
      flex-direction: column;
      width: auto;
      padding: 0vh 5vw;
    }

    .interactive .text {
      width: 100%;
    }

    .interactive h1 {
      padding-top: 2rem;
      margin-block-end: 4rem;
      font-size: 2.5rem;
    }

    .interactive p {
      font-size: 1.75rem;
    }

    .image-card {
      bottom: 100px;
      width: 100%;
    }

    .image-card img {
      width: 100%;
      border-radius: 10px;
    }
  }
</style>
